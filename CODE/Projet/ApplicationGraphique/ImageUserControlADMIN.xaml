<UserControl x:Class="ApplicationGraphique.ImageUserControlADMIN"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <WrapPanel Margin="5 5 0 0">
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <Button x:Name="DeleteButton" Style="{StaticResource Boutton-D}" Click="Button_Click_RedCrossForDeletePicUserControl" />
            <Image Style="{StaticResource Img-Margin}" Source="{Binding Link}"/>
        </Grid>
    </WrapPanel>
</UserControl>
